<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Salt" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->

        <item name="android:statusBarColor">@color/second</item>
        <item name="android:navigationBarColor">@color/second</item>
    </style>

    <style name="Theme.Salt" parent="Base.Theme.Salt" />
</resources>